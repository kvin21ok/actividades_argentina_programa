// primero ejecuta el ciclo una vez y luego va a 'preguntar' la condicion, si es true vuelve a ejecutar el ciclo hasta que sea false

let x = 10

do{
    console.log('La variable vale: '+x)
} while (x > 10);